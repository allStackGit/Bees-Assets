torch_settings:
  device: cpu
behaviors:
  BeesRL1v1:
    trainer_type: ppo
    hyperparameters:
      batch_size: 512
      buffer_size: 16384
      learning_rate: 0.00015
      # Baseline entropy pressure for continual learning. The Bees PPO
      # compatibility controller starts fresh runs higher (up to 0.004), lowers
      # beta toward this value as reward performance improves/stabilizes, and
      # raises it temporarily after a sustained reward decline. Continuous sigma
      # remains hard-capped separately so extra exploration cannot destroy
      # directional control.
      beta: 0.001
      epsilon: 0.15
      lambd: 1.0
      num_epoch: 3
      learning_rate_schedule: constant
      beta_schedule: constant
      epsilon_schedule: constant
    network_settings:
      normalize: true
      hidden_units: 512
      num_layers: 3
    reward_signals:
      extrinsic:
        gamma: 1.0
        strength: 1.0
    keep_checkpoints: 5000
    checkpoint_interval: 250000
    max_steps: 5000000000000
    time_horizon: 2048
    summary_freq: 5000
    threaded: false
    self_play:
      save_steps: 20000
      team_change: 100000
      swap_steps: 20000
      window: 30
      play_against_latest_model_ratio: 0.20
      initial_elo: 1200.0