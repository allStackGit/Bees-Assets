# Bees Learning State

Active control state for unresolved repository learning. This is not a history or general notebook. Keep it small; verified durable knowledge must move to its owner document and resolved items must be removed.

## Open learning candidates

- **Signal:** a 1.7 MB scene named `Squad Maker Aspect Ratio.unity` exists at the repository root, outside the normal `Scenes/` hierarchy, and exact-name code search found no references. **Why it may matter:** it may be an intentional aspect-ratio test/reference scene or an obsolete scratch copy; treating it incorrectly could either remove useful authored evidence or force future agents to scan duplicate scene data. **Evidence already checked:** repository tree, scene name/location, source search, current responsive-UI regression/test documentation. **Evidence still needed:** Unity Editor Build Settings/reference inspection and comparison with the canonical Squad Maker scene before classifying it as maintained test material or removable debris. **Likely owner:** `docs/TESTING.md` if intentional test material; `QUALITY_LEDGER.md`/repository cleanup if obsolete.

## Retrieval misses

_None after this seed pass; the compact context index was expanded for the broad relationships that previously required multi-file rediscovery._

## Repeated agent mistakes or workflow friction

_None left unresolved by this pass. Previously observed identity/lifecycle/pathfinding/client-server confusion is now routed explicitly through `CONTEXT_INDEX.md` and `SYSTEM_MAP.md` rather than duplicated here._

## Curation debt

_None._

## Entry format

Use a short item with: **Signal**, **Why it may matter**, **Evidence already checked**, **Evidence still needed**, and **Likely owner**. A future task should be able to resolve it without rereading the original conversation.
