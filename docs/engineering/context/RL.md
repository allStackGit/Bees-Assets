# RL / Training Route

Load this route only when a focused RL/training task needs contract or dependency context beyond the exact named source/config/test.

| Concern | Start with | Nearby evidence / boundary |
|---|---|---|
| unified / continual-learning training design, online player learning, replay/archive strategy | `Training/bees_continual_learning_rl_implementation.md` | verify implemented portions against current `Training/` and `RlOneVsOne*` code |
| dedicated ML-Agents combat training, RL 1v1, curriculum, env args | `RlOneVsOneTrainingBootstrap`, `RlOneVsOneTrainingOptions` | `RlOneVsOneAgent`, `RlOneVsOneEpisodeCoordinator`, `RlOneVsOneTrainingDurabilityGuard`, `Level.RandomSquadSetup`, `Training/rl_1v1_config.yaml`, focused EditMode tests |
| player-facing Campaign / Fish Tank RL controller selection | `RlLivePolicyAgent`, `RlLivePolicyModelBootstrap`, `Stage.ActivateBrains`, `Stage.ActivateHiveMind` | `Level.SetupHivemind`; `ActivateHiveMind` remains the AI enable/disable gate while `ActivateBrains` selects RL instead of Hive Mind; the build-staged champion `ModelAsset` is validated against the frozen policy manifest and bound with `SetModel`/`InferenceOnly`; missing, incompatible, or conflicting deployment assets clear `ActivateBrains`, disable live RL agents, and restart the existing Hive Mind path only when neural ownership was requested; player-owned Campaign ships remain player controlled |
| champion deployment packaging / build-time Unity release | `Training/bees_continual_deployment.py`, `Training/bees_continual_unity_bundle.py`, `Training/bees_continual_bootstrap.py`, `RlLivePolicyModelBootstrap`, `ContinualLearningStore.promote`, `ContinualLearningStore.rollback` | only the registry's current champion may be packaged; packaging independently verifies champion hash/compatibility/frozen signature, passing evaluation or generation-zero bootstrap evidence, re-derives attached behavior-sanity evidence from authoritative evaluation summaries, and requires real ONNX loadability; `publish` creates an immutable content-addressed package plus atomic `deployment/current-deployment.json`; promotion/rollback requires restaging only for subsequent player builds, while already-built desktop clients can receive compatible promoted champions through the authenticated/test hot-distribution path |
| automatic candidate release / deployment monitoring / rollback | `Training/bees_continual_release.py`, `Training/bees_continual_release_health.py`, `Training/bees_continual_release_rollback.py` | training already registers stable ONNX exports as candidates; one release invocation first reconciles and independently health-checks the current champion deployment, then by default processes only the newest compatible queued candidate, runs the full authoritative current-champion + full historical-league + pinned-competency evaluation, rejects only recorded failures, promotes only passing evidence, republishes, and health-checks the new champion before processing anything else; publication/health failure leaves an already-approved champion authoritative so the next invocation reconciles before touching another candidate; guarded rollback restores the previous or explicitly named historical champion, republishes it, optionally reactivates retained desktop bundles, and verifies the final release chain; an explicit-target retry is idempotent after partial release failure |
| authenticated/test live champion hot distribution | `Training/bees_continual_hot_bundle.py`, `Training/bees_continual_hot_bundle_reactivate.py`, `RlLivePolicyModelUpdater`, `RlLivePolicyModelBootstrap`, BeesServer `rlModelDistribution.js` / `security.js` | Unity builds a platform-specific AssetBundle from the verified current champion; the publisher validates deployment/model/hash/frozen ABI identity and atomically advances `current-<platform>.json`; immutable prior bundles are retained and can be reactivated after an intentional rollback only after revalidation against the newly current deployment; every ordinary desktop gameplay Stage with a valid champion anchor polls BeesServer even when Player/Hive Mind currently own all sides, Production/Development use Steam-authenticated WSS while explicit Test uses its test identity/WS boundary, and downloads pin deployment+bundle identity, verify SHA-256 plus manifest/model identity, then apply transactionally in memory; a downloaded champion is bound only when NN ownership is requested; incompatible clients fail closed, download/validation failures retain the current champion, and failed active-agent replacement rolls back or falls back to Hive Mind if rollback itself fails |
| player / Hive Mind demonstration capture and human imitation | `RlGameplayDemonstrationAgent`, `RlGameplayDemonstrationCapabilityCapture`, `Training/bees_continual_train.py` | opt-in `--rl-record-demonstrations`; shared-ABI ML-Agents `.demo` files are partitioned under `Application.persistentDataPath/RlDemonstrations/PolicyV<ABI>/{Human,HiveMind}` with a fail-closed capture manifest; successful ship specials, mining, healing and warp are emitted as isolated native event-demo episodes built synchronously from the shared policy perception/current controls at the authoritative event boundary; `--continual-human-demo-dir` requires the configured Human/PolicyV directory and exact capture behavior/ABI/policy signature, preserves the capture manifest in an immutable content-addressed snapshot, and injects configurable behavioral cloning into a generated runtime YAML; live-RL-controlled ships are excluded |
| authenticated public Human-demo upload | `RlDemonstrationUploader`, BeesServer `rlDemonstrationUploads.js` / `security.js` | separate opt-in `--rl-upload-demonstrations`; Production desktop client only; uploader snapshots only already-closed `PolicyV<ABI>/Human/*.demo` files before current-run recording begins, uses a dedicated Steam-authenticated WSS connection, sends bounded chunks, and enforces the same 16 MiB combined demo+manifest limit as server/trainer; current-run recordings stay local until a later launch; BeesServer's accepted capture policy must be bumped atomically with the Unity frozen ABI (currently v8 / 4722 observations); server content-hashes, rate-limits, connection/user-binds, and atomically quarantines bytes with `readyForTraining=false` |
| central native human-demo archive | `Training/bees_continual_native_demo.py`, `Training/bees_continual_public_demo.py`, `ContinualLearningStore.demonstration_batches` | trusted/local importer validates native ML-Agents behavior against the capture manifest, payload/record limits, known compatible model context and exact frozen signature; public importer independently verifies BeesServer quarantine metadata, hashes, exact manifest and native structure before central archival, preserves public provenance without copying raw Steam user ID, and still reports `approved_for_training=false` |
| public-demo approval / BC curation | `Training/bees_continual_demo_curation.py`, `Training/bees_continual_demo_contributors.py` | public batches require explicit immutable approval before selection; revocation permanently blocks future sets; materialization accepts an explicit batch-ID list only, enforces `human_imitation.public_min_quality_score` and `public_max_batches_per_contributor`, groups contributors with a store-local HMAC rather than raw Steam ID, and emits an immutable `PolicyV<ABI>/Human` directory accepted by `--continual-human-demo-dir`; deleting the HMAC key after contributor records exist fails closed rather than silently rotating identity |
| public live telemetry ingestion / curation / tactic mining / reviewed fresh scenario pressure | `Training/bees_continual_live_telemetry.py`, `Training/bees_continual_live_telemetry_ingest.py`, `Training/bees_continual_public_telemetry.py`, `Training/bees_continual_telemetry_curation.py`, `Training/bees_continual_telemetry_mine.py`, `Training/bees_continual_telemetry_review.py`, `Training/bees_continual_telemetry_adversarial.py` | public live telemetry is strict-schema validated, imported only from authenticated/test quarantine, archived with `trusted_for_on_policy_rl=false`, and can still be explicitly approved/revoked and materialized into immutable offline-scenario selections; Human, Hive Mind, and deployed-neural streams retain separate provenance and cannot cross-confirm each other; curated selections are mined in-memory into repeated tactical signatures and approximate geometry hints without persisting raw trajectories or raw client `agent_key` values in mining output; repetition requires distinct telemetry batches and contributor-aware mining can require cross-contributor support; because validated `agent_key` is opaque, mined self/enemy identities are perspective-local; recorded telemetry actions are never PPO trajectories, and selected pressure is capped at 50% so the current policy generates fresh on-policy rollouts |
| automatic all-gameplay telemetry / automatic tactic pressure | `RlLiveTelemetryContract`, `RlLiveTelemetryRecorder`, `RlLiveTelemetryUploader`, `RlProductionControllerRouter`, `Training/bees_continual_public_auto.py`, `Training/bees_continual_auto_train.py`, `Training/DISTRIBUTED_PUBLIC_LEARNING_SETUP.md` | every ordinary desktop gameplay Stage outside dedicated ML-Agents training automatically records bounded shared-ABI decisions from eligible Human, Hive Mind, and deployed-neural controllers unless `--rl-disable-automatic-telemetry` is used; dedicated training contributes directly through fresh PPO instead; Production/Development upload through Steam-authenticated WSS and explicit Test through the test identity/WS boundary; opaque per-match agent keys do not expose side/runtime IDs; pending files are deleted only after BeesServer confirms immutable archival; central automation independently revalidates quarantine/model/deployment/hash/schema, auto-approves only for offline mining, activates at most 32 tactic signatures and registers both side orientations where identity is opaque; default automatic target pressure is 10% under the existing 50% combined hard cap; data arriving during an active generation becomes eligible for the next immutable generation rather than mutating the current run |
| autonomous continuous train/evaluate/promote/publish loop | `Training/bees_continual_service.py`, `Training/bees_continual_service_tests.py`, `Training/DISTRIBUTED_PUBLIC_LEARNING_SETUP.md`; BeesServer `rlContinualLearningLauncher.js`, `rlContinualLearningSupervisor.js`, `start-server.js` | after one-time host configuration and generation-zero/current-champion bootstrap, BeesServer can supervise one persistent generation-bounded continual service; each generation freezes current automatic gameplay pressure, resumes one optimizer/checkpoint lineage to the next target step, runs the authoritative release gate, and publishes a platform hot bundle only for the validated current champion; phase state survives restart, internal failures retry without replacing the champion, and foreground/background server launch both provide process-level supervision; healthy operation requires no manual ingestion, curation, generation start, release, or champion publication |
| distributed ML-Agents rollout workers | `Training/bees_distributed_training.py`, `Training/bees_distributed_mlagents_learn.py`, `Training/bees_continual_distributed_train.py`, `Training/bees_remote_worker.py`, `Training/DISTRIBUTED_PUBLIC_LEARNING_SETUP.md` | one central ML-Agents 1.1.0 PPO process remains the sole optimizer/checkpoint owner; a suffix of worker IDs can wait for external Unity environments; their gRPC endpoints bind trainer loopback only and remote machines reach the same ports through SSH local forwarding; `--bees-remote-spec` is mandatory with external workers and atomically publishes a content-hashed worker/base-port/run-ID/exact-final-`--env-args` contract, including injected gameplay pressure; remote helpers validate the spec and may launch only assigned worker subsets, preventing silent environment-argument drift; every machine must still use the same training build, which remains an infrastructure/deployment prerequisite |
| player-derived adversarial training / tactic mining / tactical replay / counterplay measurement | `Training/bees_continual_adversarial_mine.py`, `Training/bees_continual_adversarial_suggest.py`, `Training/bees_continual_adversarial.py`, `Training/bees_continual_adversarial_replay.py`, `Training/bees_continual_adversarial_train.py`, `Training/bees_continual_adversarial_evaluate.py`, `RlPlayerDerivedAdversarialPressure`, `RlPlayerDerivedTacticalGeometry`, `RlPlayerDerivedActionReplay`, `RlOneVsOnePerArenaMatchups`, `RlOneVsOneArenaMapSizeState` | approved current-ABI demos can be mined into review-only explainable signatures; ABI v8 appends episode-progress/reserved values after the existing tactical fields, so current geometry/identity indices remain stable while exact observation-size validation moves to 4722; reviewed 1v1 scenarios can compile one approved Human source into an immutable bounded movement/aim/fire replay, and current replay format v2 also transports primitive ship-special/mining/healing/warp actions while retaining v1 artifact compatibility; Unity executes those capability actions without giving the scripted side PPO action/reward ownership; target-selection branches remain fail-closed because the live policy still masks them, and the separately captured authoritative capability events are isolated one-event demos that are not yet stitched into tactical prefixes; scenarios retain optional tactical map size + separation pressure, combined player-derived pressure remains capped at 50%, and paired diagnostic evaluation remains non-promotion evidence; intermediate-world-state/event stitching, target replay, and arbitrary multi-ship player episodes remain future work |
| Python trainer wrapper / batching / inference / diagnostics | `Training/bees_mlagents_learn.py` | sibling `*_tests.py`, compatibility and diagnostics modules |
| dedicated Hive Mind training vs Fish Tank | `HiveMindTrainingBootstrap` | scene `Hivemind Training`, `Stage.IsTrainingHiveMind`, random squads |
| historical ML-Agents experiment | `Brain.cs`, `Training/trainer_config.yaml` | dormant historical path; do not infer current dedicated RL or Hive Mind behavior from it |

Use exact current symbols and tests as authority. Load `PROJECT_CONSTITUTION.md`, `SYSTEM_MAP.md`, or `INVARIANTS.md` only when the RL change actually crosses a gameplay/product, lifecycle, persistence/network, or other protected boundary.
